## Nitidez.es

This is the public website for nitidez.es

## Development Server

Start the development server on `http://localhost:PORT`:

```bash
npm run dev
```

## Production

Build the application for production:

```bash
npm run build
```

Locally preview production build:

```bash
npm run preview
```
